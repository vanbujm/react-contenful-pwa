query courseQuery {
    courses(where: { courseCode: 1 }) {
      title
      locale
      image {
        url
        altText
      }
      shortDescription
      description
      duration
      skillLevel
    }
}